# v0.4.2-update 模块化重写设计文档

**日期**: 2026-06-19
**版本**: v0.4.2-update
**目标**: 代码结构优化、测试覆盖、增强事件服务器、为插件奠定基础

---

## 一、服务器架构重构

### 1.1 当前问题

- 单体 `server.js` 549 行，所有路由内联
- 缺少中间件系统，CORS 头重复
- 控制流错误（缺少闭合括号、缺少 return）
- 阻塞 I/O（`writeFileSync`）

### 1.2 新架构设计

```
server/
├── index.js              # 服务器入口，启动逻辑
├── core/
│   ├── http-server.js    # HTTP 服务器封装
│   ├── router.js         # 路由注册与匹配
│   ├── middleware.js      # 中间件管道
│   ├── context.js        # 请求上下文
│   └── websocket.js      # WebSocket 管理器
├── routes/
│   ├── health.js         # GET /health
│   ├── version.js        # GET /api/version
│   ├── generate.js       # POST /api/generate
│   ├── export.js         # POST /api/export
│   ├── config.js         # GET/PUT /api/config
│   ├── protocol.js       # POST /api/protocol
│   ├── checkpoints.js    # CRUD /api/checkpoints
│   ├── sync.js           # PUT /api/sync
│   └── events.js         # GET /api/events (WebSocket)
├── services/
│   ├── engine-service.js # 引擎调用封装
│   ├── checkpoint-service.js # 检查点管理
│   ├── config-service.js # 配置管理
│   ├── event-bus.js      # 内部事件总线
│   ├── event-store.js    # 事件持久化
│   ├── event-subscription.js # 事件订阅
│   ├── rate-limiter.js   # 流量限流
│   └── backpressure.js   # 背压控制
├── extensions/
│   └── extension-points.js # 扩展点接口
└── utils/
    ├── response.js       # JSON 响应工具
    ├── file-reader.js    # 静态文件读取（异步）
    └── etag.js           # ETag 生成
```

### 1.3 路由注册机制

```javascript
// routes/generate.js
export default {
  method: 'POST',
  path: '/api/generate',
  handler: async (ctx) => {
    const { params } = await ctx.body();
    const result = await ctx.services.engine.generate(params, ctx.services.events.emit);
    ctx.json(result);
  }
};
```

### 1.4 中间件管道

```javascript
// core/middleware.js
const middleware = [
  corsMiddleware,
  loggingMiddleware,
  errorMiddleware,
  authMiddleware,  // 插件可注册
];

export function applyMiddleware(handler, ctx) {
  return middleware.reduce(
    (next, mw) => () => mw(ctx, next),
    () => handler(ctx)
  )();
}
```

---

## 二、扩展点设计（为未来插件系统奠定基础）

> **注意**：本阶段暂不实现完整插件系统，仅保留基本扩展点设计。

### 2.1 扩展点接口

```javascript
// server/extensions/extension-points.js
export class ExtensionPoints {
  constructor() {
    this.routes = new Map();
    this.middlewares = [];
    this.eventListeners = new Map();
  }

  // 注册自定义路由
  registerRoute(method, path, handler) {
    const key = `${method}:${path}`;
    this.routes.set(key, { method, path, handler });
  }

  // 注册中间件
  registerMiddleware(mw) {
    this.middlewares.push(mw);
  }

  // 注册事件监听
  on(event, handler) {
    if (!this.eventListeners.has(event)) {
      this.eventListeners.set(event, []);
    }
    this.eventListeners.get(event).push(handler);
  }

  // 触发事件
  emit(event, data) {
    const listeners = this.eventListeners.get(event) || [];
    listeners.forEach(handler => handler(data));
  }
}
```

### 2.2 使用示例

```javascript
// 在服务器初始化时
const extensions = new ExtensionPoints();

// 注册自定义路由（未来可通过插件注册）
extensions.registerRoute('GET', '/api/custom', async (ctx) => {
  ctx.json({ message: 'Custom endpoint' });
});

// 注册事件监听
extensions.on('generation:complete', (data) => {
  console.log('Generation completed:', data);
});
```

---

## 三、事件系统增强

### 3.1 WebSocket 支持

```javascript
// server/core/websocket.js
import { WebSocketServer } from 'ws';

export class WebSocketManager {
  constructor(server) {
    this.server = server;
    this.wss = null;
    this.clients = new Map();
    this.channels = new Map();
    this.messageQueue = [];
    this.maxQueueSize = 1000;
  }

  // 初始化 WebSocket 服务器
  init(httpServer) {
    this.wss = new WebSocketServer({ server: httpServer });
    
    this.wss.on('connection', (ws, req) => {
      const clientId = this.generateClientId();
      console.log(`[WebSocket] Client connected: ${clientId}`);
      
      // 注册客户端
      this.clients.set(clientId, {
        ws,
        id: clientId,
        channels: new Set(),
        lastHeartbeat: Date.now(),
      });

      // 发送连接确认
      this.send(ws, 'connected', { clientId });

      // 处理消息
      ws.on('message', (data) => {
        this.handleMessage(clientId, data);
      });

      // 处理断开连接
      ws.on('close', () => {
        console.log(`[WebSocket] Client disconnected: ${clientId}`);
        this.clients.delete(clientId);
      });

      // 处理错误
      ws.on('error', (err) => {
        console.error(`[WebSocket] Client error: ${clientId}`, err);
        this.clients.delete(clientId);
      });
    });

    // 启动心跳检测
    this.startHeartbeat();
  }

  // 处理客户端消息
  handleMessage(clientId, data) {
    try {
      const message = JSON.parse(data);
      
      switch (message.type) {
        case 'subscribe':
          this.subscribe(clientId, message.channel);
          break;
        case 'unsubscribe':
          this.unsubscribe(clientId, message.channel);
          break;
        case 'heartbeat':
          this.handleHeartbeat(clientId);
          break;
        default:
          console.log(`[WebSocket] Unknown message type: ${message.type}`);
      }
    } catch (err) {
      console.error(`[WebSocket] Invalid message from ${clientId}:`, err);
    }
  }

  // 订阅频道
  subscribe(clientId, channel) {
    const client = this.clients.get(clientId);
    if (!client) return;

    client.channels.add(channel);
    
    if (!this.channels.has(channel)) {
      this.channels.set(channel, new Set());
    }
    this.channels.get(channel).add(clientId);
    
    console.log(`[WebSocket] Client ${clientId} subscribed to ${channel}`);
  }

  // 取消订阅
  unsubscribe(clientId, channel) {
    const client = this.clients.get(clientId);
    if (!client) return;

    client.channels.delete(channel);
    
    const channelClients = this.channels.get(channel);
    if (channelClients) {
      channelClients.delete(clientId);
      if (channelClients.size === 0) {
        this.channels.delete(channel);
      }
    }
    
    console.log(`[WebSocket] Client ${clientId} unsubscribed from ${channel}`);
  }

  // 发送消息给单个客户端
  send(ws, type, data) {
    if (ws.readyState === ws.OPEN) {
      ws.send(JSON.stringify({ type, data, timestamp: Date.now() }));
    }
  }

  // 广播消息给所有客户端
  broadcast(type, data) {
    for (const [, client] of this.clients) {
      this.send(client.ws, type, data);
    }
  }

  // 发送到特定频道
  broadcastToChannel(channel, type, data) {
    const clientIds = this.channels.get(channel) || new Set();
    for (const clientId of clientIds) {
      const client = this.clients.get(clientId);
      if (client) {
        this.send(client.ws, type, data);
      }
    }
  }

  // 启动心跳检测
  startHeartbeat() {
    setInterval(() => {
      const now = Date.now();
      for (const [clientId, client] of this.clients) {
        if (now - client.lastHeartbeat > 30000) {
          console.log(`[WebSocket] Client ${clientId} heartbeat timeout`);
          client.ws.terminate();
          this.clients.delete(clientId);
        }
      }
    }, 10000);
  }

  // 处理心跳
  handleHeartbeat(clientId) {
    const client = this.clients.get(clientId);
    if (client) {
      client.lastHeartbeat = Date.now();
      this.send(client.ws, 'heartbeat', {});
    }
  }

  // 生成客户端 ID
  generateClientId() {
    return `client_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
  }

  // 获取连接统计
  getStats() {
    return {
      totalClients: this.clients.size,
      totalChannels: this.channels.size,
      channels: Object.fromEntries(
        Array.from(this.channels.entries()).map(([channel, clients]) => [
          channel,
          clients.size,
        ])
      ),
    };
  }
}
```

### 3.2 事件订阅机制

```javascript
// server/services/event-subscription.js
export class EventSubscription {
  constructor(eventBus) {
    this.eventBus = eventBus;
    this.subscriptions = new Map();
    this.filters = new Map();
  }

  // 订阅事件
  subscribe(channel, event, handler, filter = null) {
    const subscriptionId = this.generateId();
    
    if (!this.subscriptions.has(channel)) {
      this.subscriptions.set(channel, new Map());
    }
    
    this.subscriptions.get(channel).set(subscriptionId, {
      event,
      handler,
      filter,
      createdAt: Date.now(),
    });

    // 注册到事件总线
    this.eventBus.on(event, (data) => {
      this.dispatchEvent(channel, event, data);
    });

    console.log(`[EventSubscription] Subscribed: ${channel}/${event}`);
    return subscriptionId;
  }

  // 取消订阅
  unsubscribe(subscriptionId) {
    for (const [channel, subs] of this.subscriptions) {
      if (subs.has(subscriptionId)) {
        subs.delete(subscriptionId);
        console.log(`[EventSubscription] Unsubscribed: ${subscriptionId}`);
        return true;
      }
    }
    return false;
  }

  // 分发事件
  dispatchEvent(channel, event, data) {
    const subs = this.subscriptions.get(channel);
    if (!subs) return;

    for (const [, subscription] of subs) {
      if (subscription.event !== event) continue;
      
      // 应用过滤器
      if (subscription.filter && !subscription.filter(data)) {
        continue;
      }

      try {
        subscription.handler(data);
      } catch (err) {
        console.error(`[EventSubscription] Handler error:`, err);
      }
    }
  }

  // 获取订阅统计
  getStats() {
    let totalSubscriptions = 0;
    for (const subs of this.subscriptions.values()) {
      totalSubscriptions += subs.size;
    }
    return {
      channels: this.subscriptions.size,
      totalSubscriptions,
    };
  }

  // 生成订阅 ID
  generateId() {
    return `sub_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
  }
}
```

### 3.3 事件持久化

```javascript
// server/services/event-store.js
import { writeFile, readFile, readdir } from 'fs/promises';
import { join } from 'path';

export class EventStore {
  constructor(config) {
    this.config = config;
    this.events = [];
    this.maxEvents = config.maxEvents || 10000;
    this.storagePath = config.storagePath || './events';
  }

  // 初始化存储
  async init() {
    try {
      await readdir(this.storagePath);
    } catch (err) {
      await writeFile(this.storagePath, '[]', 'utf-8');
    }
  }

  // 存储事件
  async store(event) {
    const eventWithMeta = {
      ...event,
      id: this.generateEventId(),
      timestamp: Date.now(),
      stored: true,
    };

    this.events.push(eventWithMeta);

    // 限制内存中的事件数量
    if (this.events.length > this.maxEvents) {
      this.events.shift();
    }

    // 异步持久化到文件
    await this.persist();

    return eventWithMeta;
  }

  // 获取事件
  getEvent(eventId) {
    return this.events.find(e => e.id === eventId);
  }

  // 获取事件历史
  getHistory(options = {}) {
    const { channel, event, since, limit = 100 } = options;
    
    let filtered = this.events;

    if (channel) {
      filtered = filtered.filter(e => e.channel === channel);
    }

    if (event) {
      filtered = filtered.filter(e => e.type === event);
    }

    if (since) {
      filtered = filtered.filter(e => e.timestamp >= since);
    }

    return filtered.slice(-limit);
  }

  // 重放事件
  async replay(options = {}) {
    const { since, channel, event } = options;
    const events = this.getHistory({ since, channel, event });
    
    for (const event of events) {
      console.log(`[EventStore] Replaying: ${event.type}`);
      // 这里可以触发事件总线
    }

    return events.length;
  }

  // 持久化到文件
  async persist() {
    try {
      const data = JSON.stringify(this.events, null, 2);
      await writeFile(this.storagePath, data, 'utf-8');
    } catch (err) {
      console.error('[EventStore] Persist error:', err);
    }
  }

  // 从文件加载
  async load() {
    try {
      const data = await readFile(this.storagePath, 'utf-8');
      this.events = JSON.parse(data);
    } catch (err) {
      console.error('[EventStore] Load error:', err);
      this.events = [];
    }
  }

  // 清除旧事件
  async cleanup(maxAge = 86400000) { // 默认 24 小时
    const cutoff = Date.now() - maxAge;
    this.events = this.events.filter(e => e.timestamp > cutoff);
    await this.persist();
  }

  // 生成事件 ID
  generateEventId() {
    return `evt_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
  }

  // 获取统计信息
  getStats() {
    return {
      totalEvents: this.events.length,
      oldestEvent: this.events[0]?.timestamp,
      newestEvent: this.events[this.events.length - 1]?.timestamp,
    };
  }
}
```

### 3.4 流量控制

```javascript
// server/services/rate-limiter.js
export class RateLimiter {
  constructor(config = {}) {
    this.windowMs = config.windowMs || 60000; // 1 分钟
    this.maxRequests = config.maxRequests || 100;
    this.clients = new Map();
    this.messageQueue = [];
    this.maxQueueSize = config.maxQueueSize || 1000;
  }

  // 检查客户端是否允许发送
  isAllowed(clientId) {
    const client = this.clients.get(clientId);
    
    if (!client) {
      // 新客户端
      this.clients.set(clientId, {
        count: 1,
        windowStart: Date.now(),
      });
      return true;
    }

    // 检查窗口是否过期
    if (Date.now() - client.windowStart > this.windowMs) {
      // 重置窗口
      client.count = 1;
      client.windowStart = Date.now();
      return true;
    }

    // 检查是否超过限制
    if (client.count >= this.maxRequests) {
      return false;
    }

    client.count++;
    return true;
  }

  // 入队消息
  enqueue(clientId, message) {
    if (this.messageQueue.length >= this.maxQueueSize) {
      // 队列已满，丢弃最旧的消息
      this.messageQueue.shift();
    }

    this.messageQueue.push({
      clientId,
      message,
      timestamp: Date.now(),
    });
  }

  // 出队消息
  dequeue() {
    return this.messageQueue.shift();
  }

  // 获取队列大小
  getQueueSize() {
    return this.messageQueue.length;
  }

  // 清理过期客户端
  cleanup() {
    const now = Date.now();
    for (const [clientId, client] of this.clients) {
      if (now - client.windowStart > this.windowMs) {
        this.clients.delete(clientId);
      }
    }
  }

  // 获取统计信息
  getStats() {
    return {
      totalClients: this.clients.size,
      queueSize: this.messageQueue.length,
      windowMs: this.windowMs,
      maxRequests: this.maxRequests,
    };
  }
}

// server/services/backpressure.js
export class BackpressureController {
  constructor(config = {}) {
    this.highWaterMark = config.highWaterMark || 100;
    this.lowWaterMark = config.lowWaterMark || 10;
    this.paused = false;
    this.pendingMessages = 0;
    this.callbacks = new Map();
  }

  // 检查是否应该暂停
  shouldPause() {
    return this.pendingMessages >= this.highWaterMark;
  }

  // 检查是否应该恢复
  shouldResume() {
    return this.paused && this.pendingMessages <= this.lowWaterMark;
  }

  // 暂停
  pause(clientId) {
    this.paused = true;
    console.log(`[Backpressure] Paused: ${clientId}`);
  }

  // 恢复
  resume(clientId) {
    this.paused = false;
    console.log(`[Backpressure] Resumed: ${clientId}`);
  }

  // 增加待处理消息
  increment() {
    this.pendingMessages++;
    
    if (this.shouldPause()) {
      this.pause('system');
    }
  }

  // 减少待处理消息
  decrement() {
    this.pendingMessages--;
    
    if (this.shouldResume()) {
      this.resume('system');
    }
  }

  // 获取状态
  getStatus() {
    return {
      paused: this.paused,
      pendingMessages: this.pendingMessages,
      highWaterMark: this.highWaterMark,
      lowWaterMark: this.lowWaterMark,
    };
  }
}
```

### 3.5 内部事件总线

```javascript
// server/services/event-bus.js
import { EventEmitter } from 'events';

export class EventBus extends EventEmitter {
  constructor() {
    super();
    this.setMaxListeners(50);
    this.eventHistory = [];
    this.maxHistory = 1000;
  }

  // 带日志的事件触发
  emit(event, data) {
    console.log(`[EventBus] ${event}`, data);
    
    // 记录事件历史
    this.eventHistory.push({
      event,
      data,
      timestamp: Date.now(),
    });
    
    // 限制历史记录大小
    if (this.eventHistory.length > this.maxHistory) {
      this.eventHistory.shift();
    }
    
    return super.emit(event, data);
  }

  // 异步事件触发
  async emitAsync(event, data) {
    const listeners = this.listeners(event);
    for (const listener of listeners) {
      await listener(data);
    }
  }

  // 获取事件历史
  getHistory(event = null, limit = 100) {
    let history = this.eventHistory;
    
    if (event) {
      history = history.filter(h => h.event === event);
    }
    
    return history.slice(-limit);
  }

  // 清除历史
  clearHistory() {
    this.eventHistory = [];
  }
}
```

### 3.6 事件类型定义

```javascript
// 事件类型常量
export const EVENTS = {
  // 引擎事件
  PHASE_START: 'phase:start',
  PHASE_COMPLETE: 'phase:complete',
  PHASE_ERROR: 'phase:error',
  PROGRESS: 'progress',
  GENERATION_START: 'generation:start',
  GENERATION_COMPLETE: 'generation:complete',
  GENERATION_ERROR: 'generation:error',

  // 检查点事件
  CHECKPOINT_SAVED: 'checkpoint:saved',
  CHECKPOINT_LOADED: 'checkpoint:loaded',
  CHECKPOINT_DELETED: 'checkpoint:deleted',
  CHECKPOINT_SYNCED: 'checkpoint:synced',

  // 配置事件
  CONFIG_CHANGED: 'config:changed',
  PROTOCOL_CHANGED: 'protocol:changed',

  // WebSocket 事件
  WS_CLIENT_CONNECTED: 'ws:client:connected',
  WS_CLIENT_DISCONNECTED: 'ws:client:disconnected',
  WS_MESSAGE_RECEIVED: 'ws:message:received',

  // 服务器事件
  SERVER_READY: 'server:ready',
  SERVER_ERROR: 'server:error',
  SERVER_SHUTDOWN: 'server:shutdown',

  // 系统事件
  SYSTEM_ERROR: 'system:error',
  SYSTEM_WARNING: 'system:warning',
};
```

---

## 四、引擎模块重构

### 4.1 问题修复

| 问题 | 修复方案 |
|------|----------|
| `PERM` 全局状态 | 改为 `NoiseEngine` 实例属性 |
| `Math.random()` 非确定性 | 使用 `seededRandom()` 替代 |
| `generateLakes()` 重复 | 统一到 `rivers.js`，`erosion.js` 引用 |
| 生物群系分类重复 | 提取到 `regions.js`，统一调用 |
| `rivers.js` 缺少导入 | 添加 `createNoise` 导入 |

### 4.2 新引擎架构

```
engine/
├── index.js              # 生成流水线编排
├── noise.js              # 噪声引擎（实例化）
├── tectonic.js           # 板块生成
├── erosion.js            # 侵蚀模拟
├── rivers.js             # 河流追踪
├── regions.js            # 生物群系分类
├── phases/               # 阶段定义
│   ├── base-phase.js     # 阶段基类
│   ├── noise-phase.js    # 噪声阶段
│   ├── tectonic-phase.js # 板块阶段
│   ├── erosion-phase.js  # 侵蚀阶段
│   ├── river-phase.js    # 河流阶段
│   └── region-phase.js   # 区域阶段
└── registry.js           # 阶段注册表
```

### 4.3 阶段注册机制

```javascript
// engine/phases/base-phase.js
export class BasePhase {
  constructor(name, weight = 1) {
    this.name = name;
    this.weight = weight;
  }

  async execute(context) {
    throw new Error('Phase must implement execute()');
  }

  validate(input) {
    return true;
  }
}

// engine/registry.js
export class PhaseRegistry {
  constructor() {
    this.phases = new Map();
  }

  register(phase) {
    this.phases.set(phase.name, phase);
  }

  get(name) {
    return this.phases.get(name);
  }

  getAll() {
    return Array.from(this.phases.values())
      .sort((a, b) => a.weight - b.weight);
  }
}
```

---

## 五、客户端重构

### 5.1 模块化拆分

```
public/js/
├── app.js                # 入口，初始化
├── core/
│   ├── state.js          # 全局状态管理
│   ├── events.js         # SSE 连接管理
│   └── config.js         # 配置管理
├── ui/
│   ├── controls.js       # 参数控件
│   ├── drawer.js         # 侧边栏
│   ├── checkpoint.js     # 检查点 UI
│   └── mobile.js         # 移动端适配
├── renderer/
│   ├── index.js          # 渲染器抽象
│   ├── progressive.js    # 渐进式渲染
│   ├── webgl.js          # WebGL2 渲染
│   └── canvas2d.js       # Canvas2D 回退
├── engine/               # 引擎模块（共享）
│   └── ...（保持不变）
└── utils/
    ├── i18n.js           # 国际化
    └── dom.js            # DOM 工具
```

### 5.2 状态管理

```javascript
// core/state.js
export class AppState {
  constructor() {
    this.listeners = new Map();
    this.state = {
      mode: 'server',
      isGenerating: false,
      currentPhase: null,
      progress: 0,
      checkpoints: [],
      config: {},
    };
  }

  get(key) {
    return this.state[key];
  }

  set(key, value) {
    const old = this.state[key];
    this.state[key] = value;
    this.emit(key, { old, new: value });
  }

  on(key, handler) {
    if (!this.listeners.has(key)) {
      this.listeners.set(key, []);
    }
    this.listeners.get(key).push(handler);
  }

  emit(event, data) {
    const handlers = this.listeners.get(event) || [];
    handlers.forEach(h => h(data));
  }
}
```

---

## 六、测试覆盖扩展

### 6.1 测试结构

```
tests/
├── unit/
│   ├── noise.test.js      # 已有
│   ├── tectonic.test.js   # 已有
│   ├── erosion.test.js    # 已有
│   ├── regions.test.js    # 新增
│   ├── rivers.test.js     # 新增
│   ├── event-bus.test.js  # 新增
│   ├── websocket.test.js  # 新增
│   ├── rate-limiter.test.js # 新增
│   ├── event-store.test.js # 新增
│   └── phase-registry.test.js # 新增
├── integration/
│   ├── api.test.js        # 已有
│   ├── server.test.js     # 已有
│   ├── checkpoint-api.test.js # 新增
│   ├── websocket-api.test.js # 新增
│   └── sse-events.test.js # 新增
└── e2e/
    ├── generation.test.js # 新增
    └── mobile.test.js     # 新增
```

### 6.2 测试用例设计

```javascript
// tests/unit/websocket.test.js
import { describe, it } from 'node:test';
import assert from 'node:assert';
import { WebSocketManager } from '../../server/core/websocket.js';

describe('WebSocketManager', () => {
  describe('subscribe()', () => {
    it('should subscribe client to channel', () => {
      // 测试频道订阅
    });

    it('should handle multiple subscriptions', () => {
      // 测试多个订阅
    });
  });

  describe('broadcastToChannel()', () => {
    it('should send message to all channel subscribers', () => {
      // 测试频道广播
    });

    it('should not send to non-subscribers', () => {
      // 测试非订阅者不接收消息
    });
  });
});

// tests/unit/rate-limiter.test.js
describe('RateLimiter', () => {
  describe('isAllowed()', () => {
    it('should allow requests within limit', () => {
      // 测试允许范围内的请求
    });

    it('should reject requests over limit', () => {
      // 测试拒绝超限请求
    });

    it('should reset window after expiry', () => {
      // 测试窗口重置
    });
  });
});

// tests/unit/event-store.test.js
describe('EventStore', () => {
  describe('store()', () => {
    it('should store events with metadata', () => {
      // 测试存储事件
    });

    it('should persist to file', () => {
      // 测试持久化
    });
  });

  describe('getHistory()', () => {
    it('should return filtered events', () => {
      // 测试过滤事件
    });

    it('should respect limit parameter', () => {
      // 测试限制参数
    });
  });
});

// tests/integration/websocket-api.test.js
describe('WebSocket API', () => {
  it('should establish connection', () => {
    // 测试建立连接
  });

  it('should handle subscribe/unsubscribe', () => {
    // 测试订阅/取消订阅
  });

  it('should receive broadcast messages', () => {
    // 测试接收广播消息
  });

  it('should handle heartbeat', () => {
    // 测试心跳处理
  });
});
```

---

## 七、实现步骤

### 阶段 1：服务器重构（核心）

1. 创建 `server/` 目录结构
2. 实现路由系统
3. 实现中间件管道
4. 迁移现有路由
5. 添加异步文件操作
6. 修复控制流错误

### 阶段 2：事件系统增强

1. 实现 WebSocket 管理器
2. 实现事件订阅机制
3. 实现事件持久化存储
4. 实现流量控制（限流 + 背压）
5. 增强内部事件总线
6. 迁移 SSE 到 WebSocket

### 阶段 3：扩展点设计

1. 实现基本扩展点接口
2. 保留路由、中间件、事件注册能力
3. 为未来插件系统奠定基础

### 阶段 4：引擎重构

1. 修复全局状态问题
2. 替换 `Math.random()`
3. 统一 `generateLakes()`
4. 实现阶段注册机制

### 阶段 5：客户端重构

1. 拆分 `app.js`
2. 实现状态管理
3. 实现 WebSocket 客户端
4. 优化渲染器
5. 添加 i18n 支持

### 阶段 6：测试扩展

1. 添加单元测试
2. 添加集成测试
3. 添加 E2E 测试
4. 提升覆盖率到 80%+

---

## 八、详细技术实现

### 8.1 服务器核心实现

#### HTTP 服务器封装

```javascript
// server/core/http-server.js
import { createServer } from 'http';
import { Router } from './router.js';
import { Middleware } from './middleware.js';
import { Context } from './context.js';
import { WebSocketManager } from './websocket.js';

export class HTTPServer {
  constructor(config) {
    this.config = config;
    this.router = new Router();
    this.middleware = new Middleware();
    this.wsManager = new WebSocketManager(this);
    this.server = null;
    this.services = {};
    this.stats = {
      requests: 0,
      errors: 0,
      startTime: null,
    };
  }

  // 注册服务
  registerService(name, service) {
    this.services[name] = service;
  }

  // 启动服务器
  async start() {
    this.server = createServer(async (req, res) => {
      const ctx = new Context(req, res, this);
      
      try {
        this.stats.requests++;
        await this.middleware.execute(ctx);
        
        if (!ctx.responseSent) {
          const route = this.router.match(req.method, ctx.path);
          if (route) {
            ctx.params = route.params;
            await route.handler(ctx);
          } else {
            ctx.notFound();
          }
        }
      } catch (err) {
        this.stats.errors++;
        ctx.error(err);
      }
    });

    // 初始化 WebSocket
    this.wsManager.init(this.server);

    return new Promise((resolve) => {
      this.server.listen(this.config.port, this.config.host, () => {
        this.stats.startTime = Date.now();
        resolve();
      });
    });
  }

  // 关闭服务器
  async stop() {
    return new Promise((resolve) => {
      if (this.server) {
        this.server.close(resolve);
      } else {
        resolve();
      }
    });
  }

  // 获取统计信息
  getStats() {
    return {
      ...this.stats,
      uptime: this.stats.startTime ? Date.now() - this.stats.startTime : 0,
      websocket: this.wsManager.getStats(),
    };
  }
}
```

#### 路由系统

```javascript
// server/core/router.js
export class Router {
  constructor() {
    this.routes = new Map();
    this.patterns = new Map();
  }

  // 注册路由
  register(method, path, handler) {
    // 检查是否有动态参数
    if (path.includes(':')) {
      const pattern = this.pathToRegex(path);
      this.patterns.set(`${method}:${pattern.key}`, { pattern, handler, method });
    } else {
      this.routes.set(`${method}:${path}`, { path, handler, method });
    }
  }

  // 匹配路由
  match(method, path) {
    // 精确匹配
    const exact = this.routes.get(`${method}:${path}`);
    if (exact) return exact;

    // 模式匹配
    for (const [, route] of this.patterns) {
      if (route.method !== method) continue;
      const match = path.match(route.pattern.regex);
      if (match) {
        const params = {};
        route.pattern.params.forEach((param, i) => {
          params[param] = match[i + 1];
        });
        return { ...route, params };
      }
    }

    return null;
  }

  // 路径转正则
  pathToRegex(path) {
    const params = [];
    const regex = path.replace(/:(\w+)/g, (_, name) => {
      params.push(name);
      return '([^/]+)';
    });
    return { regex: new RegExp(`^${regex}$`), params };
  }
}

// 便捷注册方法
Router.prototype.get = function(path, handler) {
  this.register('GET', path, handler);
};

Router.prototype.post = function(path, handler) {
  this.register('POST', path, handler);
};

Router.prototype.put = function(path, handler) {
  this.register('PUT', path, handler);
};

Router.prototype.delete = function(path, handler) {
  this.register('DELETE', path, handler);
};
```

#### 中间件系统

```javascript
// server/core/middleware.js
export class Middleware {
  constructor() {
    this.middlewares = [];
  }

  // 添加中间件
  use(mw) {
    this.middlewares.push(mw);
  }

  // 执行中间件链
  async execute(ctx) {
    let index = 0;
    const next = async () => {
      if (index < this.middlewares.length) {
        const mw = this.middlewares[index++];
        await mw(ctx, next);
      }
    };
    await next();
  }
}

// 内置中间件
export const corsMiddleware = async (ctx, next) => {
  ctx.setHeader('Access-Control-Allow-Origin', '*');
  ctx.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS');
  ctx.setHeader('Access-Control-Allow-Headers', 'Content-Type');
  
  if (ctx.method === 'OPTIONS') {
    ctx.status(204).end();
    return;
  }
  
  await next();
};

export const loggingMiddleware = async (ctx, next) => {
  const start = Date.now();
  await next();
  const duration = Date.now() - start;
  console.log(`${ctx.method} ${ctx.path} ${ctx.status} ${duration}ms`);
};

export const errorMiddleware = async (ctx, next) => {
  try {
    await next();
  } catch (err) {
    ctx.error(err);
  }
};
```

#### 请求上下文

```javascript
// server/core/context.js
import { createReadStream } from 'fs';
import { stat } from 'fs/promises';
import { extname } from 'path';

export class Context {
  constructor(req, res, server) {
    this.req = req;
    this.res = res;
    this.server = server;
    this.services = server.services;
    this.params = {};
    this.responseSent = false;
  }

  // 获取请求路径
  get path() {
    return this.req.url.split('?')[0];
  }

  // 获取查询参数
  get query() {
    const url = new URL(this.req.url, `http://${this.req.headers.host}`);
    return Object.fromEntries(url.searchParams);
  }

  // 获取请求方法
  get method() {
    return this.req.method;
  }

  // 设置响应头
  setHeader(name, value) {
    this.res.setHeader(name, value);
  }

  // 设置状态码
  status(code) {
    this.res.statusCode = code;
    return this;
  }

  // 发送 JSON 响应
  json(data) {
    if (this.responseSent) return;
    this.responseSent = true;
    
    this.res.writeHead(this.res.statusCode || 200, {
      'Content-Type': 'application/json',
    });
    this.res.end(JSON.stringify(data));
  }

  // 发送错误响应
  error(err) {
    if (this.responseSent) return;
    this.responseSent = true;
    
    const status = err.status || 500;
    this.res.writeHead(status, {
      'Content-Type': 'application/json',
    });
    this.res.end(JSON.stringify({
      error: err.message,
      status,
    }));
  }

  // 发送 404
  notFound() {
    this.error({ status: 404, message: 'Not Found' });
  }

  // 发送静态文件
  async sendFile(filePath) {
    try {
      const fileStat = await stat(filePath);
      const ext = extname(filePath);
      const mimeTypes = {
        '.html': 'text/html',
        '.css': 'text/css',
        '.js': 'application/javascript',
        '.json': 'application/json',
        '.png': 'image/png',
        '.jpg': 'image/jpeg',
        '.svg': 'image/svg+xml',
      };

      this.res.writeHead(200, {
        'Content-Type': mimeTypes[ext] || 'application/octet-stream',
        'Content-Length': fileStat.size,
      });

      createReadStream(filePath).pipe(this.res);
    } catch (err) {
      this.notFound();
    }
  }

  // 解析请求体
  async body() {
    return new Promise((resolve, reject) => {
      let data = '';
      this.req.on('data', chunk => {
        data += chunk;
      });
      this.req.on('end', () => {
        try {
          resolve(JSON.parse(data));
        } catch (err) {
          reject(err);
        }
      });
      this.req.on('error', reject);
    });
  }
}
```

### 8.2 扩展点详细实现

#### 扩展点接口

```javascript
// server/extensions/extension-points.js
export class ExtensionPoints {
  constructor() {
    this.routes = new Map();
    this.middlewares = [];
    this.eventListeners = new Map();
    this.phases = [];
    this.renderStyles = new Map();
  }

  // 注册自定义路由
  registerRoute(method, path, handler) {
    const key = `${method}:${path}`;
    this.routes.set(key, { method, path, handler });
  }

  // 注册中间件
  registerMiddleware(mw) {
    this.middlewares.push(mw);
  }

  // 注册事件监听
  on(event, handler) {
    if (!this.eventListeners.has(event)) {
      this.eventListeners.set(event, []);
    }
    this.eventListeners.get(event).push(handler);
  }

  // 触发事件
  emit(event, data) {
    const listeners = this.eventListeners.get(event) || [];
    listeners.forEach(handler => handler(data));
  }

  // 注册引擎阶段
  registerPhase(phase) {
    this.phases.push(phase);
  }

  // 注册渲染风格
  registerRenderStyle(style) {
    this.renderStyles.set(style.name, style);
  }

  // 获取所有注册的路由
  getRoutes() {
    return Array.from(this.routes.values());
  }

  // 获取所有注册的中间件
  getMiddlewares() {
    return this.middlewares;
  }

  // 获取所有注册的渲染风格
  getRenderStyles() {
    return Array.from(this.renderStyles.values());
  }
}
```

### 8.3 引擎阶段详细实现

#### 阶段基类

```javascript
// engine/phases/base-phase.js
export class BasePhase {
  constructor(name, weight = 1) {
    this.name = name;
    this.weight = weight;
    this.startTime = null;
    this.endTime = null;
  }

  // 执行阶段（子类必须实现）
  async execute(context) {
    throw new Error(`Phase ${this.name} must implement execute()`);
  }

  // 验证输入
  validate(input) {
    return true;
  }

  // 包装执行（带计时和事件）
  async wrappedExecute(context) {
    this.startTime = Date.now();
    
    // 发送阶段开始事件
    context.emit('phase:start', { 
      phase: this.name, 
      weight: this.weight 
    });

    try {
      // 验证输入
      if (!this.validate(context)) {
        throw new Error(`Phase ${this.name}: invalid input`);
      }

      // 执行阶段
      const result = await this.execute(context);
      
      this.endTime = Date.now();
      
      // 发送阶段完成事件
      context.emit('phase:complete', {
        phase: this.name,
        duration: this.endTime - this.startTime,
        result,
      });

      return result;
    } catch (err) {
      this.endTime = Date.now();
      
      // 发送阶段错误事件
      context.emit('phase:error', {
        phase: this.name,
        error: err.message,
        duration: this.endTime - this.startTime,
      });

      throw err;
    }
  }
}
```

#### 噪声阶段

```javascript
// engine/phases/noise-phase.js
import { BasePhase } from './base-phase.js';
import { NoiseEngine } from '../noise.js';

export class NoisePhase extends BasePhase {
  constructor() {
    super('noise', 1);
  }

  validate(context) {
    return context.params && 
           typeof context.params.seed === 'number' &&
           typeof context.params.width === 'number' &&
           typeof context.params.height === 'number';
  }

  async execute(context) {
    const { seed, width, height } = context.params;
    
    // 创建噪声引擎实例
    const noise = new NoiseEngine(seed);
    
    // 生成噪声图
    const noiseMap = noise.fbm(width, height, {
      octaves: context.params.octaves || 6,
      lacunarity: context.params.lacunarity || 2.0,
      persistence: context.params.persistence || 0.5,
    });

    // 更新上下文
    context.data = context.data || {};
    context.data.noiseMap = noiseMap;
    context.data.noise = noise;

    return { noiseMap };
  }
}
```

#### 板块阶段

```javascript
// engine/phases/tectonic-phase.js
import { BasePhase } from './base-phase.js';
import { generatePlates, assignPlates, computeBoundaries } from '../tectonic.js';

export class TectonicPhase extends BasePhase {
  constructor() {
    super('tectonic', 2);
  }

  validate(context) {
    return context.data && 
           context.data.noiseMap &&
           context.params &&
           typeof context.params.plateCount === 'number';
  }

  async execute(context) {
    const { width, height, plateCount } = context.params;
    const { noiseMap, noise } = context.data;

    // 生成板块
    const plates = generatePlates(noise, width, height, plateCount);
    
    // 分配板块
    const plateMap = assignPlates(plates, width, height);
    
    // 计算边界
    const boundaries = computeBoundaries(plateMap, width, height);

    // 更新上下文
    context.data.plates = plates;
    context.data.plateMap = plateMap;
    context.data.boundaries = boundaries;

    return { plates, plateMap, boundaries };
  }
}
```

#### 侵蚀阶段

```javascript
// engine/phases/erosion-phase.js
import { BasePhase } from './base-phase.js';
import { generateElevation, hydraulicErosion, generateLakes } from '../erosion.js';

export class ErosionPhase extends BasePhase {
  constructor() {
    super('erosion', 3);
  }

  validate(context) {
    return context.data && 
           context.data.plateMap &&
           context.data.boundaries;
  }

  async execute(context) {
    const { width, height, seed } = context.params;
    const { plateMap, boundaries, noise } = context.data;

    // 生成初始高程
    let elevation = generateElevation(plateMap, boundaries, width, height);
    
    // 水力侵蚀
    elevation = hydraulicErosion(elevation, width, height, {
      iterations: context.params.erosionIterations || 100000,
      seed,
    });

    // 生成湖泊
    const lakes = generateLakes(elevation, width, height);

    // 更新上下文
    context.data.elevation = elevation;
    context.data.lakes = lakes;

    return { elevation, lakes };
  }
}
```

#### 河流阶段

```javascript
// engine/phases/river-phase.js
import { BasePhase } from './base-phase.js';
import { generateRivers } from '../rivers.js';

export class RiverPhase extends BasePhase {
  constructor() {
    super('river', 4);
  }

  validate(context) {
    return context.data && 
           context.data.elevation &&
           context.data.lakes;
  }

  async execute(context) {
    const { width, height, seed } = context.params;
    const { elevation, lakes } = context.data;

    // 生成河流
    const rivers = generateRivers(elevation, lakes, width, height, {
      riverCount: context.params.riverCount || 10,
      seed,
    });

    // 更新上下文
    context.data.rivers = rivers;

    return { rivers };
  }
}
```

#### 区域阶段

```javascript
// engine/phases/region-phase.js
import { BasePhase } from './base-phase.js';
import { analyzeRegions, computeClimate } from '../regions.js';

export class RegionPhase extends BasePhase {
  constructor() {
    super('region', 5);
  }

  validate(context) {
    return context.data && 
           context.data.elevation &&
           context.data.lakes;
  }

  async execute(context) {
    const { width, height } = context.params;
    const { elevation, lakes, rivers } = context.data;

    // 计算气候
    const climate = computeClimate(elevation, width, height);
    
    // 分析区域
    const regions = analyzeRegions(elevation, climate, lakes, rivers, width, height);

    // 更新上下文
    context.data.climate = climate;
    context.data.regions = regions;

    return { climate, regions };
  }
}
```

#### 阶段注册表

```javascript
// engine/registry.js
export class PhaseRegistry {
  constructor() {
    this.phases = new Map();
    this.executionOrder = [];
  }

  // 注册阶段
  register(phase) {
    if (this.phases.has(phase.name)) {
      throw new Error(`Phase already registered: ${phase.name}`);
    }
    
    this.phases.set(phase.name, phase);
    this.updateExecutionOrder();
    
    console.log(`[PhaseRegistry] Registered: ${phase.name} (weight: ${phase.weight})`);
  }

  // 获取阶段
  get(name) {
    return this.phases.get(name);
  }

  // 获取所有阶段（按权重排序）
  getAll() {
    return this.executionOrder;
  }

  // 更新执行顺序
  updateExecutionOrder() {
    this.executionOrder = Array.from(this.phases.values())
      .sort((a, b) => a.weight - b.weight);
  }

  // 执行所有阶段
  async executeAll(context) {
    const results = {};
    
    for (const phase of this.executionOrder) {
      try {
        const result = await phase.wrappedExecute(context);
        results[phase.name] = result;
      } catch (err) {
        console.error(`[PhaseRegistry] Phase ${phase.name} failed:`, err);
        throw err;
      }
    }
    
    return results;
  }

  // 获取阶段统计
  getStats() {
    const stats = {};
    for (const [name, phase] of this.phases) {
      stats[name] = {
        weight: phase.weight,
        duration: phase.endTime ? phase.endTime - phase.startTime : null,
      };
    }
    return stats;
  }
}
```

### 8.4 客户端模块详细实现

#### 状态管理

```javascript
// public/js/core/state.js
export class AppState {
  constructor() {
    this.state = {
      mode: 'server',
      isGenerating: false,
      currentPhase: null,
      progress: 0,
      checkpoints: [],
      config: {},
      mapData: null,
      renderer: null,
    };
    
    this.listeners = new Map();
    this.history = [];
  }

  // 获取状态
  get(key) {
    return this.state[key];
  }

  // 设置状态
  set(key, value) {
    const old = this.state[key];
    this.state[key] = value;
    
    // 记录历史
    this.history.push({
      key,
      old,
      new: value,
      timestamp: Date.now(),
    });
    
    // 触发监听器
    this.emit(key, { old, new: value });
  }

  // 监听状态变化
  on(key, handler) {
    if (!this.listeners.has(key)) {
      this.listeners.set(key, []);
    }
    this.listeners.get(key).push(handler);
    
    // 返回取消监听函数
    return () => {
      const handlers = this.listeners.get(key);
      const index = handlers.indexOf(handler);
      if (index > -1) {
        handlers.splice(index, 1);
      }
    };
  }

  // 触发事件
  emit(event, data) {
    const handlers = this.listeners.get(event) || [];
    handlers.forEach(h => h(data));
    
    // 触发通配符监听器
    const wildcardHandlers = this.listeners.get('*') || [];
    wildcardHandlers.forEach(h => h(event, data));
  }

  // 获取状态历史
  getHistory() {
    return this.history;
  }

  // 重置状态
  reset() {
    this.state = {
      mode: 'server',
      isGenerating: false,
      currentPhase: null,
      progress: 0,
      checkpoints: [],
      config: {},
      mapData: null,
      renderer: null,
    };
    this.history = [];
  }
}
```

#### WebSocket 客户端

```javascript
// public/js/core/websocket-client.js
export class WebSocketClient {
  constructor(url) {
    this.url = url;
    this.ws = null;
    this.listeners = new Map();
    this.reconnectAttempts = 0;
    this.maxReconnectAttempts = 5;
    this.reconnectDelay = 1000;
    this.heartbeatInterval = null;
  }

  // 连接
  connect() {
    this.ws = new WebSocket(this.url);
    
    this.ws.onopen = () => {
      console.log('[WebSocket] Connected');
      this.reconnectAttempts = 0;
      this.emit('connected', {});
      
      // 启动心跳
      this.startHeartbeat();
    };

    this.ws.onmessage = (event) => {
      try {
        const message = JSON.parse(event.data);
        this.emit(message.type, message.data);
      } catch (err) {
        console.error('[WebSocket] Parse error:', err);
      }
    };

    this.ws.onclose = (event) => {
      console.log('[WebSocket] Disconnected', event.code, event.reason);
      this.stopHeartbeat();
      this.emit('disconnected', { code: event.code, reason: event.reason });
      
      // 自动重连
      if (this.reconnectAttempts < this.maxReconnectAttempts) {
        this.reconnectAttempts++;
        const delay = this.reconnectDelay * Math.pow(2, this.reconnectAttempts - 1);
        console.log(`[WebSocket] Reconnecting in ${delay}ms (attempt ${this.reconnectAttempts})`);
        setTimeout(() => this.connect(), delay);
      }
    };

    this.ws.onerror = (err) => {
      console.error('[WebSocket] Error:', err);
      this.emit('error', { error: err });
    };
  }

  // 发送消息
  send(type, data) {
    if (this.ws && this.ws.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify({ type, data }));
    }
  }

  // 订阅频道
  subscribe(channel) {
    this.send('subscribe', { channel });
  }

  // 取消订阅
  unsubscribe(channel) {
    this.send('unsubscribe', { channel });
  }

  // 监听事件
  on(event, handler) {
    if (!this.listeners.has(event)) {
      this.listeners.set(event, []);
    }
    this.listeners.get(event).push(handler);
  }

  // 触发事件
  emit(event, data) {
    const handlers = this.listeners.get(event) || [];
    handlers.forEach(h => h(data));
  }

  // 启动心跳
  startHeartbeat() {
    this.heartbeatInterval = setInterval(() => {
      this.send('heartbeat', {});
    }, 30000);
  }

  // 停止心跳
  stopHeartbeat() {
    if (this.heartbeatInterval) {
      clearInterval(this.heartbeatInterval);
      this.heartbeatInterval = null;
    }
  }

  // 断开连接
  disconnect() {
    this.stopHeartbeat();
    if (this.ws) {
      this.ws.close();
      this.ws = null;
    }
  }
}
```

#### 渲染器抽象

```javascript
// public/js/renderer/index.js
export class Renderer {
  constructor(canvas) {
    this.canvas = canvas;
    this.ctx = canvas.getContext('2d');
    this.styles = new Map();
    this.currentStyle = null;
  }

  // 注册渲染风格
  registerStyle(style) {
    this.styles.set(style.name, style);
  }

  // 设置渲染风格
  setStyle(name) {
    const style = this.styles.get(name);
    if (style) {
      this.currentStyle = style;
    }
  }

  // 渲染
  render(data) {
    if (!this.currentStyle) {
      throw new Error('No render style selected');
    }

    // 清空画布
    this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height);
    
    // 调用当前风格的渲染函数
    this.currentStyle.render(this.ctx, data);
  }

  // 获取可用风格
  getAvailableStyles() {
    return Array.from(this.styles.keys());
  }
}

// public/js/renderer/webgl-renderer.js
export class WebGLRenderer extends Renderer {
  constructor(canvas) {
    super(canvas);
    this.gl = canvas.getContext('webgl2');
    this.program = null;
    this.textures = new Map();
  }

  // 初始化着色器
  initShaders(vertexSource, fragmentSource) {
    const vertexShader = this.compileShader(this.gl.VERTEX_SHADER, vertexSource);
    const fragmentShader = this.compileShader(this.gl.FRAGMENT_SHADER, fragmentSource);
    
    this.program = this.gl.createProgram();
    this.gl.attachShader(this.program, vertexShader);
    this.gl.attachShader(this.program, fragmentShader);
    this.gl.linkProgram(this.program);
    
    if (!this.gl.getProgramParameter(this.program, this.gl.LINK_STATUS)) {
      throw new Error('Shader link failed');
    }
  }

  // 编译着色器
  compileShader(type, source) {
    const shader = this.gl.createShader(type);
    this.gl.shaderSource(shader, source);
    this.gl.compileShader(shader);
    
    if (!this.gl.getShaderParameter(shader, this.gl.COMPILE_STATUS)) {
      throw new Error('Shader compile failed');
    }
    
    return shader;
  }

  // 创建纹理
  createTexture(name, data, width, height) {
    const texture = this.gl.createTexture();
    this.gl.bindTexture(this.gl.TEXTURE_2D, texture);
    this.gl.texImage2D(
      this.gl.TEXTURE_2D,
      0,
      this.gl.RGBA,
      width,
      height,
      0,
      this.gl.RGBA,
      this.gl.UNSIGNED_BYTE,
      data
    );
    this.gl.texParameteri(this.gl.TEXTURE_2D, this.gl.TEXTURE_MIN_FILTER, this.gl.LINEAR);
    this.gl.texParameteri(this.gl.TEXTURE_2D, this.gl.TEXTURE_MAG_FILTER, this.gl.LINEAR);
    
    this.textures.set(name, texture);
    return texture;
  }

  // 渲染
  render(data) {
    if (!this.program) {
      throw new Error('Shaders not initialized');
    }

    this.gl.useProgram(this.program);
    
    // 绑定纹理
    for (const [name, texture] of this.textures) {
      const location = this.gl.getUniformLocation(this.program, name);
      this.gl.uniform1i(location, 0);
      this.gl.activeTexture(this.gl.TEXTURE0);
      this.gl.bindTexture(this.gl.TEXTURE_2D, texture);
    }
    
    // 绘制
    this.gl.drawArrays(this.gl.TRIANGLE_STRIP, 0, 4);
  }
}
```

---

## 九、风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 功能回归 | 高 | 完整测试套件，逐步迁移 |
| 性能下降 | 中 | 性能基准测试，优化关键路径 |
| WebSocket 兼容性 | 中 | 降级到 SSE，渐进增强 |
| 事件持久化开销 | 中 | 异步写入，批量处理 |
| 客户端兼容性 | 低 | 渐进增强，降级方案 |

---

## 十、成功标准

- [ ] 服务器代码行数减少 50%+
- [ ] 模块职责单一，易于理解
- [ ] WebSocket 双向通信正常工作
- [ ] 事件订阅机制灵活可用
- [ ] 事件持久化和重放功能正常
- [ ] 流量控制（限流 + 背压）有效
- [ ] 测试覆盖率达到 80%+
- [ ] 所有现有功能正常工作
- [ ] 性能无显著下降
