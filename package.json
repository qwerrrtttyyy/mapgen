{
  "name": "mapgen-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Material Map Generator - Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "packageManager": "npm@10.0.0"
}
