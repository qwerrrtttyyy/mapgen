{
  "name": "mapgen-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Material Map Generator - Pure Frontend Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "puppeteer-core": "^25.2.1",
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "packageManager": "npm@10.0.0",
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
